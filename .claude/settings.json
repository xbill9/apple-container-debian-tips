{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // .tool_response.filePath'); case \"$f\" in \"$CLAUDE_PROJECT_DIR\"/bin/*) { bash -n \"$f\" && shellcheck \"$f\"; } >&2 || exit 2 ;; esac",
            "timeout": 30,
            "statusMessage": "Checking shell script"
          }
        ]
      }
    ]
  }
}
